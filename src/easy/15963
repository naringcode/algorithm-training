#include <iostream>

using namespace std;

long long a;
long long b;

int main()
{
    cin >> a >> b;

    if (a == b)
    {
        cout << 1;
    }
    else
    {
        cout << 0;
    }

    return 0;
}
