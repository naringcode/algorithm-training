#include <bits/stdc++.h>

using namespace std;

long long n;
long long m;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> n >> m;

    cout << abs(n - m);

    return 0;
}