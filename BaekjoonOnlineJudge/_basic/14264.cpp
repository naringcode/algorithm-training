#include <bits/stdc++.h>

using namespace std;

using ll = long long;

double l;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> l;

    cout.precision(9);

    println(cout, "{}", sqrt(3.0) * (l * l) / 4);

    return 0;
}
