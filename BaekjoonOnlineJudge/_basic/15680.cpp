#include <bits/stdc++.h>

using namespace std;

int n;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> n;

    if (0 == n)
    {
        cout << "YONSEI";
    }
    else
    {
        cout << "Leading the Way to the Future";
    }

    return 0;
}
