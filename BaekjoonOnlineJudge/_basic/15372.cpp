#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int t;
int n;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> t;

    while (t--)
    {
        ll temp;
        cin >> temp;

        println("{}", temp * temp);
    }

    return 0;
}
